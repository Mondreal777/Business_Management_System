"use strict"
// Libraries
// const _ = require('lodash'); -- use this only if file has mapping

/**
 *  module.exports = { INSTANT_MEETING, SCHEDULED_MEETING, RECURRING_MEETING_NO_FIXED_TIME, RECURRING_MEETING_WITH_FIXED_TIME };
 * or
 * module.exports = { getTaskLabelByStatus };
 */

const ASCENDING = {
	label: 'ASC'
};

const DESCENDING = {
    label: 'DESC'
}

module.exports = {
	ASCENDING, DESCENDING
};